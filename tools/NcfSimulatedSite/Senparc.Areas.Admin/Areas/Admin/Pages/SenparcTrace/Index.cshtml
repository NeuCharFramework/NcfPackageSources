@page
@model Senparc.Areas.Admin.Areas.Admin.Pages.SenparcTrace_IndexModel
@{
    ViewData["Title"] = AR["Trace.Log"];
    Layout = "_Layout_Vue";
}
@section breadcrumbs {
    <el-breadcrumb-item>@AR["Common.SystemManagement"]</el-breadcrumb-item>
    <el-breadcrumb-item>@AR["Trace.Log"]</el-breadcrumb-item>
}

<div>
    <el-table :data="tableData"
              border
              style="width: 100%">
        <el-table-column align="center"
                         label="@AR["Trace.Number"]"
                         width="120">
            <template slot-scope="scope">
                {{ scope.row.no}}
            </template>
        </el-table-column>
        <el-table-column align="center"
                         label="@AR["Trace.Date"]">
            <template slot-scope="scope">
                <a :href="'/Admin/SenparcTrace/DateLog?date=' + scope.row.text">
                    {{ scope.row.text }}
                </a>
            </template>
        </el-table-column>
    </el-table>
    <pagination :total="searchData.total"
                :page.sync="searchData.pageIndex"
                :limit.sync="searchData.pageSize"
                @@pagination="fetchData"></pagination>
</div>

@section scripts{
    <script src="~/js/Admin/Pages/SenparcTrace/Index.js"></script>
}

