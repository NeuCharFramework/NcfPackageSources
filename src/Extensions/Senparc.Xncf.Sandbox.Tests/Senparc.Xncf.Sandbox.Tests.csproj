<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Version>0.0.4</Version>
    <PackageReleaseNotes>
      [2026-08-13] v0.0.1 Dependency update from Senparc.Xncf.Sandbox to 0.1.1-preview2
      [2026-08-15] v0.0.2 增加 Sandbox 预览路径回归测试
      1、覆盖隔离预览路径和基路径约束
      2、补充镜像解析、Jupyter 路径、会话状态与模板目录回归测试
      3、补充会话 TTL/永久保持策略与状态回归测试
      4、Dependency update from Senparc.Xncf.Sandbox to 0.2.0
      [2026-08-22] v0.0.3 补充沙箱运行时与 TTL 策略回归测试
      1、覆盖模板目录、Jupyter 路径、工作区状态、运行时解析和永久保持会话策略
      [2026-08-29] v0.0.4 Dependency updates from Senparc.Xncf.Sandbox to 0.3.0
      1、Dependency update from Senparc.Xncf.Sandbox to 0.3.0
    </PackageReleaseNotes>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="MSTest.TestAdapter" Version="3.6.1" />
    <PackageReference Include="MSTest.TestFramework" Version="3.6.1" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Senparc.Xncf.Sandbox\Senparc.Xncf.Sandbox.csproj" />
  </ItemGroup>
</Project>
