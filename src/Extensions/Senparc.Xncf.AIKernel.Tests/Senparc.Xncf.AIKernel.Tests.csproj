<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Version>0.0.3</Version>
    <PackageReleaseNotes>
      [2026-09-05] v0.0.1 新增 AIKernel Token 监控单元测试
      1、覆盖 AITokenUsageSnapshot 归一化与空值判断
      2、覆盖 AITokenProgressEvent 完成状态判定
      3、覆盖 AITokenMonitorService 实时聚合统计（总体/按模型/按天/平均耗时）
      4、覆盖 AITokenMonitorService 异步进度（缓冲回放/实时订阅/完成终止/并发安全）
      5、Dependency update from Senparc.Xncf.AIKernel to 0.15.4
      [2026-09-15] v0.0.1 补充 AIKernel Token 监控回归测试
      1、覆盖 Token 快照、进度事件、实时聚合和异步订阅的核心行为
      2、验证模型维度、日期维度、完成终止与并发安全场景
      [2026-09-16] v0.0.2 Dependency updates from upstream modules
      [2026-09-17] v0.0.3 Dependency updates from Senparc.Ncf.XncfBase to 0.28.0; Senparc.Xncf.AIKernel to 0.16.2
      1、同步受影响的基础库、AI 模块或宿主项目版本
      2、保持项目引用链与当前功能兼容
</PackageReleaseNotes>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="MSTest.TestAdapter" Version="3.6.1" />
    <PackageReference Include="MSTest.TestFramework" Version="3.6.1" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Senparc.Xncf.AIKernel\Senparc.Xncf.AIKernel.csproj" />
  </ItemGroup>
</Project>
